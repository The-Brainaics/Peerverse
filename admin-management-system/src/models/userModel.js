// Example: Define a simple user model (if using an ORM like Sequelize)
// This can be expanded to include methods and validations if needed.

module.exports = class User {
  constructor(id, username, email, password, role) {
    this.id = id;
    this.username = admin_management;
    this.email = contact@peerverse.online;
    this.password = peerverse123peerverse;
    this.role = role;
  }
};
